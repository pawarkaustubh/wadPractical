<!-- filepath: mongo_employee_girish/views/edit.ejs -->
<!DOCTYPE html>
<html>
<head>
  <title>Edit Employee</title>
  <!-- Add this to the <head> of each EJS file -->
<style>
    body {
      font-family: Arial, sans-serif;
      background: #f4f4f4;
      margin: 0;
      padding: 20px;
    }
    h1 {
      text-align: center;
      color: #333;
    }
    table {
      border-collapse: collapse;
      width: 80%;
      margin: 20px auto;
      background: #fff;
    }
    th, td {
      border: 1px solid #ccc;
      padding: 10px 15px;
      text-align: center;
    }
    th {
      background: #eee;
    }
    a, button {
      padding: 5px 10px;
      margin: 2px;
      text-decoration: none;
      color: #fff;
      background: #007bff;
      border: none;
      border-radius: 3px;
      cursor: pointer;
    }
    a:hover, button:hover {
      background: #0056b3;
    }
    form {
      width: 300px;
      margin: 30px auto;
      background: #fff;
      padding: 20px;
      border-radius: 5px;
      box-shadow: 0 2px 5px rgba(0,0,0,0.05);
    }
    input[type="text"], input[type="number"], input[type="date"] {
      width: 95%;
      padding: 8px;
      margin: 8px 0;
      border: 1px solid #ccc;
      border-radius: 3px;
    }
    button[type="submit"] {
      width: 100%;
      background: #28a745;
    }
    a[href="/"] {
      display: inline-block;
      margin-top: 10px;
      background: #6c757d;
    }
  </style>
</head>
<body>
  <h1>Edit Employee</h1>
  <form action="/edit/<%= employee.Name %>" method="POST">
    <input type="text" name="Name" value="<%= employee.Name %>" required><br>
    <input type="text" name="Department" value="<%= employee.Department %>" required><br>
    <input type="text" name="Designation" value="<%= employee.Designation %>" required><br>
    <input type="number" name="Salary" value="<%= employee.Salary %>" required><br>
    <input type="date" name="Joining_Date" value="<%= employee.Joining_Date ? employee.Joining_Date.toISOString().slice(0,10) : '' %>" required><br>
    <button type="submit">Update</button>
  </form>
  <a href="/">Back</a>
</body>
</html>